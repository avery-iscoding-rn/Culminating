#ifndef PLANE_HPP
#define PLANE_HPP


#include <iostream>
//#include <algorthim>
#include <string>
#include <vector>
#include <cmath>
#include "Point.hpp"

class Plane {
private:
    Point coordinates;
    bool flying;
    int capacity, speed;
public:
    Plane(Point ll, bool f, int cp, int s)
    Point::getDistance();
}
#endif